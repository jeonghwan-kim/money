extends layout

block content
	.container
		h1.page-header
			a(href="#")
				| Money
		.jumbotron
			form(method="POST" action="/signin")
				input.form-control.input-lg(type="email" name="email" placeholder="Email address" required autofocus)
				input.form-control.input-lg(type="password" name="password" placeholder="Password" required)
				label.checkbox
					input(type="checkbox")
					| 기억하기
				button.btn.btn-lg.btn-primary(type="submit") 로그인
			a.btn.btn-lg.btn-default#signin-btn(href="/signup" role="button")
				| 회원가입