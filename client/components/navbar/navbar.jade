div.navbar.navbar-default.navbar-static-top(ng-controller='NavbarCtrl')
  div.container
    div.navbar-header
      button.navbar-toggle(type='button', ng-click='isCollapsed = !isCollapsed')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') 가계부

    div#navbar-main.navbar-collapse.collapse(collapse='isCollapsed')
      ul.nav.navbar-nav
        li(ng-repeat='item in menu', ng-class='{active: isActive(item.link)}')
          a(ng-href='{{item.link}}') {{item.title}}
      ul.nav.navbar-nav.navbar-right
        li(ng-hide="isLoggedIn()" ng-class="{active: isActive('/register')}"): a(href="/register") 회원가입
        li(ng-hide="isLoggedIn()" ng-class="{active: isActive('/login')}"): a(href="/login") 로그인
        li(ng-show="isLoggedIn()"): p.navbar-text 안녕 {{getCurrentUser().name}}
        li(ng-show="isLoggedIn()" ng-class="{active: isActive('/reset-password')}"): a(href="/reset-password") 비번변경
        li(ng-show="isLoggedIn()" ng-class="{active: isActive('/logout')}"): a(href="" ng-click="logout()") 로그아웃
