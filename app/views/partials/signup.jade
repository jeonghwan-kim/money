.container
  h1.title
    a(href="/")
      | OMG
      span(style="font-size:95%") 가계부
  .jumbotron
    h3.text-muted
      span.glyphicon.glyphicon-user
      | &ensp;회원가입
    form(ng-submit="submit()")
      .form-group
        input.form-control.input-lg(type="email" ng-model="email" name="email" placeholder="새로운 이메일 주소" required autofocus)
      .form-group
        input.form-control.input-lg(type="password" ng-model="password" name="password" placeholder="새로운 비밀번호." required)
      hr.brand-gray-light
      .form-group
        .btn-group.btn-group-justified
          .btn-group
            a.btn.btn-lg.btn-default.btn-block(ng-href="/signin")
              span.glyphicon.glyphicon-arrow-left
              | &ensp;뒤로가기
          .btn-group
            button.btn.btn-lg.btn-primary.btn-block(type="submit") 계정 만들기
      p {{msg}}